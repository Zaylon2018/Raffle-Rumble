{
  "name": "raffle-rumble-render",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "(cd client && npm install) && (cd server && npm install)",
    "prebuild": "npm run install:all",
    "build": "cd client && npm install vite && node node_modules/vite/bin/vite.js build",
    "start": "node server/index.js"
  }
}
